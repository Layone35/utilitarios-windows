{
  "identifier": "default",
  "description": "Capability padrão do Control Pro",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
